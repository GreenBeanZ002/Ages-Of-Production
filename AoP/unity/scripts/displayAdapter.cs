using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Display_adjuster : MonoBehaviour
{
//    public ResourceHandler resourceHandler; // Reference to ResourceHandler script
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
//        int resourcePrice = resourcehandler.resourcePrice_House;
//        Display_adjuster.Price.Text = "Costs" + resourcePrice.ToString();
    }
}
